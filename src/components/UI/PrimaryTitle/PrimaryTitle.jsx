import React from "react";
import cl from "./PrimaryTitle.module.css";

const PrimaryTitle = () => {
  return (
    <h1 className={cl.primaryTitle}>Мы международная транспортная компания</h1>
  );
};

export default PrimaryTitle;
